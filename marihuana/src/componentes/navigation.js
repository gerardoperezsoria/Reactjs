import React, { Component } from 'react';

class Navigation extends Component {
  render() {
    return (
      <nav className="padre">
        <a className="header">
           Tasks
        </a>
      </nav>
    );
  }
}

export default Navigation;